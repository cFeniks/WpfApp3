<Page x:Class="WpfApp3.Pages.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Позиции заказов"
      Loaded="OrdersPage_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Позиции заказов"
                   FontSize="24"
                   FontWeight="Bold"
                   VerticalAlignment="Center"/>

        <StackPanel x:Name="AdminButtonsPanel"
                    Grid.Row="1"
                    Orientation="Horizontal">

            <Button Content="Добавить позицию"
                    Width="120"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="AddButton_Click"/>

            <Button Content="Редактировать"
                    Width="120"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="EditButton_Click"/>

            <Button Content="Удалить"
                    Width="100"
                    Height="30"
                    Click="DeleteButton_Click"/>
        </StackPanel>

        <ListBox x:Name="OrdersList"
                 Grid.Row="2"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White"
                            BorderBrush="#DDDDDD"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="15"
                            Margin="0,5">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Article, StringFormat=Артикул заказа: {0}}"
                                           FontWeight="Bold"
                                           FontSize="14"/>
                                <TextBlock Text="{Binding Status, StringFormat=Статус: {0}}"
                                           Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding PickupPointAddress, StringFormat=Пункт выдачи: {0}}"
                                           Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding OrderDate, StringFormat=Дата заказа: {0:dd.MM.yyyy}}"
                                           Margin="0,4,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Right"
                                        Margin="20,0,0,0">
                                <TextBlock Text="Дата доставки"
                                           FontSize="11"
                                           Foreground="Gray"
                                           HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding DeliveryDate, StringFormat={}{0:dd.MM.yyyy}}"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>